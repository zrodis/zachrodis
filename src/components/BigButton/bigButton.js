import React from "react"

export default (BigButton) => {


    return(
      <BigButton
        id={this.props.id}
        show={show === this.props.id}
        handleExpansion={handleExpansion}
        handleCollapse={handleCollapse}
        {...props}
       />
    )

}
